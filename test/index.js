'use strict';
import assert from 'assert';
import yalLog from '../lib';

/*global describe */
/*global it */

describe('yal-log', function () {
  it('should have unit tests!', function () {
    assert(false, 'we expected this package author to add actual unit tests - and he didn\'t. Bad author! Bad!.');
  });
});
